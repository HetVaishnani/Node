<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Add Book Form with Image</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }

        .card {
            max-width: 600px;
            margin: 50px auto;
            border-radius: 15px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
        }

        .form-container {
            max-width: 600px;
            margin: 50px auto;
            background-color: #ffffff;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        }

        .form-title {
            text-align: center;
            margin-bottom: 25px;
            font-weight: bold;
            color: #343a40;
        }

        .btn-add {
            width: 100%;
        }

        .image-preview {
            width: 100%;
            max-height: 300px;
            object-fit: contain;
            border: 1px solid #ccc;
            border-radius: 10px;
            margin-top: 10px;
        }

        label.required::after {
            content: '*';
            color: red;
            margin-left: 5px;
        }
    </style>
</head>

<body>

    <div class="form-container">
        <h3 class="form-title">📘 Add New Book</h3>
        <form action="/add" method="post">
            <div class="mb-3">
                <label for="bookNumber" class="form-label required">Book Number</label>
                <input type="number" class="form-control" id="bookNumber" name="number" placeholder="Enter book number"
                    required>
            </div>

            <div class="mb-3">
                <label for="bookImage" class="form-label required">Book Image</label>
                <input type="text" class="form-control" id="bookImage" name="image" placeholder="Enter book image URL"
                    accept="image/*" required>
            </div>

            <div class="mb-3">
                <label for="bookName" class="form-label required">Book Name</label>
                <input type="text" class="form-control" id="bookName" name="name" placeholder="Enter book name"
                    required>
            </div>

            <div class="mb-3">
                <label for="authorName" class="form-label required">Author Name</label>
                <input type="text" class="form-control" id="authorName" name="author" placeholder="Enter author name"
                    required>
            </div>

            <div class="mb-3">
                <label for="price" class="form-label required">Price ($)</label>
                <input type="number" class="form-control" id="price" name="price" placeholder="Enter price" step="0.01"
                    required>
            </div>

            <button type="submit" class="btn btn-success btn-add">+ Add Book</button>
        </form>
    </div>


    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>