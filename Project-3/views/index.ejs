<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Book Store Navbar</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .navbar-custom {
            background-color: #343a40;
        }

        .navbar-brand {
            font-size: 1.7rem;
            font-weight: bold;
            color: #ffffff;
            display: flex;
            align-items: center;
            gap: 10px;
            letter-spacing: 1px;
        }

        .logo-icon {
            font-size: 2rem;
            color: #ffc107;
            animation: spin 4s linear infinite;
        }

        @keyframes spin {
            0% {
                transform: rotate(0deg);
            }

            100% {
                transform: rotate(360deg);
            }
        }

        .btn-add-book {
            background-color: #28a745;
            color: white;
        }

        .btn-add-book:hover {
            background-color: #218838;
        }

        .book-card {
            max-width: 300px;
            margin: 20px auto;
            border-radius: 15px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .book-card img {
            height: 350px;
            width: auto;
            object-fit: cover;
        }

        .card-title {
            font-size: 1.2rem;
            font-weight: bold;
            color: #343a40;
        }

        .card-text {
            font-size: 0.95rem;
        }

        .btn-group .btn {
            width: 50%;
        }
    </style>
</head>

<body>

    <nav class="navbar navbar-expand-lg navbar-dark navbar-custom shadow-sm">
        <div class="container-fluid d-flex justify-content-between align-items-center">
            <a class="navbar-brand" href="#">
                <span class="logo-icon">📖</span>
                <span>BookNest</span>
            </a>
            <a href="/book"><button class="btn btn-add-book" type="button">+ Add Book</button></a>
        </div>
    </nav>
    <!-- <%book.map((e,i)=>{%>
        <div class="container">
            <div class="card book-card col-sm-6 col-md-4 col-lg-3">
                <img src="<%=e.image%>" class="card-img-top" alt="Book Image">
                <div class="card-body">
                    <h5 class="card-title"><strong>Name :</strong>
                        <%=e.name%>
                    </h5>
                    <p class="card-text"><strong>Author :</strong>
                        <%=e.author%>
                    </p>
                    <p class="card-text"><strong>Price :</strong> $<%=e.price%>
                    </p>
                </div>
                <div class="card-footer bg-white border-top-0 d-flex">
                    <a href="/editbook?id=<%=e.id%>"><button class="btn btn-warning rounded-3 me-3 ">Edit</button></a>
                    <a href="/deletebook?id=<%=e.id%>"><button class="btn btn-danger rounded-3 ">Delete</button></a>
                </div>
            </div>
        </div>
        <%})%> -->
    <div class="container my-4">
        <div class="row g-4">
            <% book.map((e, i)=> { %>
                <div class="col-sm-6 col-md-4 col-lg-3">
                    <div class="card book-card h-100">
                        <img src="<%= e.image %>" class="card-img-top" alt="Book Image">
                        <div class="card-body">
                            <h5 class="card-title"><strong>Name:</strong>
                                <%= e.name %>
                            </h5>
                            <p class="card-text"><strong>Author:</strong>
                                <%= e.author %>
                            </p>
                            <p class="card-text"><strong>Price:</strong> $<%= e.price %>
                            </p>
                        </div>
                        <div class="card-footer bg-white border-top-0 d-flex justify-content-between">
                            <a href="/editbook?id=<%= e.id %>" class="btn btn-warning btn-sm rounded-3">Edit</a>
                            <a href="/deletebook?id=<%= e.id %>" class="btn btn-danger btn-sm rounded-3">Delete</a>
                        </div>
                    </div>
                </div>
                <% }) %>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>